'use strict';

import {greet, invertCase} from '../js/app'; //import the functions to test!

/* tests go here */
describe('Checking invertCase', function() {
  it('should all be uppercase', function() {
    expect(document.querySelector('d-none')).toEqual(null);
  });
});
